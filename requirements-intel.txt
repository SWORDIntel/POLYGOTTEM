# POLYGOTTEM - Intel Meteor Lake Optimization
# Optional Intel Hardware Acceleration
#
# WARNING: These packages are large (1-5GB total) and require Intel hardware
# Only install if you have Intel Core Ultra (Meteor Lake) or Arc GPU

# First install core requirements
-r requirements.txt

# Intel Hardware Acceleration
# -----------------------------------------------------------------------

# OpenVINO - Intel NPU acceleration for Meteor Lake Core Ultra
# Provides neural processing unit support for pattern operations
openvino>=2024.0.0

# Intel Level Zero - Low-latency GPU access for Intel Arc iGPU
# Direct hardware access for Arc Graphics (Xe-LPG)
# NOTE: Level Zero is a SYSTEM package, install via:
#   Ubuntu/Debian: sudo apt install intel-level-zero-gpu level-zero
#   Fedora/RHEL: sudo dnf install level-zero
# PyOpenCL will automatically use Level Zero if available

# PyOpenCL - GPU acceleration for Intel Arc and OpenCL-compatible GPUs
# Works with Intel Arc iGPU and other OpenCL devices
pyopencl>=2024.1

# Intel Extension for Scikit-learn - Optimized NumPy/SciPy operations
# Automatically accelerates NumPy/SciPy operations on Intel hardware
scikit-learn-intelex>=2024.0

# Intel Neural Compressor - Model optimization for NPU
# Further optimizations for AI workloads on NPU
neural-compressor>=2.5

# Installation Instructions
# -------------------------
#
# Basic installation (CPU only - RECOMMENDED):
#   ./install.sh
#
# Intel Meteor Lake optimized installation (NPU + Arc GPU):
#   ./install.sh --intel
#
# Check acceleration status:
#   ./launch.sh --benchmark
#
# Performance Notes
# -----------------
# - With NPU: ~10-50x speedup for XOR operations
# - With Arc GPU: ~5-20x speedup for parallel processing
# - CPU only: Uses optimized Python bytearrays (still fast!)
